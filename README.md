offregister_gopherci
===============
This package follows the offregister specification to setup and configure GopherCI

## Install dependencies

    pip install -r requirements.txt

## Install package

    pip install .
